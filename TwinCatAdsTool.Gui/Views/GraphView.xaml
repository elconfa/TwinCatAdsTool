<UserControl x:Class="TwinCatAdsTool.Gui.Views.GraphView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:oxy="http://oxyplot.org/wpf"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:properties="clr-namespace:TwinCatAdsTool.Gui.Properties"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <oxy:PlotView Grid.Row="0"
                      Margin="12,12,12,0"
                      Background="Transparent"
                      Model="{Binding PlotModel}" />

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="12,8,12,12">
            <TextBlock Text="{x:Static properties:Resources.DisplayedTimespan}"
                       Style="{StaticResource SecondaryText}"
                       Margin="0,0,8,0" />
            <xctk:TimeSpanUpDown Value="{Binding ExpiresAfter}" MinWidth="110" />
        </StackPanel>
    </Grid>
</UserControl>
